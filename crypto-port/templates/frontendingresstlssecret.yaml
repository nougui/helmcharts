{{- if .Values.frontendIngress.tls }}
{{- range .Values.frontendIngress.tls }}
apiVersion: v1
kind: Secret
metadata:
  name: {{ .secretName }}
  {{- with .labels }}
  labels:
    {{- toYaml . | nindent 4 }}
  {{- end }}
type: kubernetes.io/tls
data:
  tls.crt: {{ .crt }}
  tls.key: {{ .key }}
{{- end }}
{{- end }}